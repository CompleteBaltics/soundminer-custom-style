div#search {
  & form#frm-search {
    @apply w-full;

    & #div-search-container {
      @apply flex items-center bg-transparent w-full max-w-none;
      &:before {
        content: 'FEEL FREE TO START TYPING HERE';
        @apply text-2xl font-secondary text-center font-medium text-white mr-8;
      }
    }

    & #btn-search-fields, & #search-filter-flag, & #btn-clear-dropdown, & #btn-lock {
      @apply hidden;
    }

    & #search-text {
      @apply top-0 bg-transparent text-2xl font-secondary text-center font-medium text-white mr-3 flex-1 flex-grow text-left border-b-2 border-orange;
      border-bottom: solid;
      border-bottom-color: #ff6400;

      &::placeholder {
        @apply text-white opacity-35 uppercase;
      }
    }

    & .search-top-area {
      & #search-btns-container {
        @apply flex flex-row;

        &>div {
          @apply bg-transparent w-18 h-18 justify-center items-center ml-5 bg-center bg-no-repeat border-none rounded-full;
          background-position: center!important;
          transition: background-color 300ms;

          & span {
            @apply hidden;
          }

          &#btn-search {
            background-image: url("http://massive.soundchute.com/custom/search.png")!important;
          }
          &#btn-small-search-advanced {
            background-image: url("http://massive.soundchute.com/custom/target.png");
            @apply ml-0;
          }

          &:hover {
            background-color: rgba(255, 255, 255, 0.1);
          }
        }
      }
    }
  }

  & #facets-icon-facets-main-container {
    @apply left-0 items-center bottom-auto mt-4;

    &[style="display: block;"] {
      display: flex!important;
    }
    
    & #clear-facets-icon {
      @apply font-primary font-bold text-white text-base font-bold uppercase m-0 float-none items-center;

      &[style="display: block;"] {
        display: flex!important;
      }

      & .icon {
        width: 0.813rem;
        height: 0.813rem;
        @apply mr-2 relative;
        background: transparent;

        &:after, &:before {
          content: '';
          @apply left-1/2 top-1/2 absolute h-2px w-full bg-white transform -translate-x-1/2 -translate-y-1/2;
        }

        &:after {
          @apply rotate-45;
        }

        &:before {
          @apply -rotate-45;
        }
      }
    }

    & #wrap-facets-new {
      margin-left: 18.7rem;
      margin-right: 11rem;

      & #facets-parsed {
        @apply flex flex-wrap items-center;

        & .prs-facet-container {
          @apply border-none p-0 rounded-none m-0 mr-5 bg-transparent text-white text-base font-primary uppercase font-bold inline-flex flex-row-reverse items-center;

          & .del-parsed-facet {
            @apply overflow-hidden inline-block mr-2 relative;
            width: 0.813rem;
            height: 0.813rem;
            text-indent: -9999px;

            &:after, &:before {
              content: '';
              @apply left-1/2 top-1/2 absolute h-2px w-full bg-white transform -translate-x-1/2 -translate-y-1/2;
            }

            &:after {
              @apply rotate-45;
            }

            &:before {
              @apply -rotate-45;
            }
          }

          & .prs-term {
            @apply no-underline;
          }
        }
      }
    }
  }
}

#as_search-text {
  @apply bg-black text-white p-6;

  & ul.as_ul_text_title li.as_li_warning_playlist {
    @apply text-red text-xl font-secondary font-black mb-6;
    color: #ff3700!important;
  }

  & ul {
    @apply rounded-none;
  }

  & #as_ul {
    @apply pl-0;

    & li {
      &.as_highlight {
        & a {
          @apply bg-red;
        }
      }
      & a {
        @apply text-white;
      }
    }
  }
}